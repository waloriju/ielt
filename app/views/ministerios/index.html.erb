<h1>Ministérios</h1>

<table class="table">
  <tr>
    <th>Titulo</th>
    <th>Descrição</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ministerios.each do |ministerio| %>
  <tr>
    <td><%= ministerio.titulo %></td>
    <td><%= ministerio.descricao %></td>
    <td><%= link_to 'Show', ministerio %></td>
    <td><%= link_to 'Edit', edit_ministerio_path(ministerio) %></td>
    <td><%= link_to 'Destroy', ministerio, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ministerio', new_ministerio_path %>
