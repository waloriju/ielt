<!DOCTYPE html>
<html lang="en">
<head>
	<title>IELT</title>
	<%= stylesheet_link_tag    "application", :media => "all" %>
	<%= javascript_include_tag "application" %>
	<link href='http://fonts.googleapis.com/css?family=Open+Sans:400,700' rel='stylesheet' type='text/css'>
	<link href='http://fonts.googleapis.com/css?family=Lobster' rel='stylesheet' type='text/css'>
	<%= csrf_meta_tags %>
</head>
<body>

	<div class="container">
		<div class="navbar">
			<div class="navbar-inner">
				<a class="brand" href="/">IELT</a>
				<ul class="nav">
					<li><a href="#">Sobre a IEL</a></li>
					<li><a href="/noticias">Noticias</a></li>
					<li class="dropdown">
						<a href="#" role="button" class="dropdown-toggle" data-toggle="dropdown">Ministérios <b class="caret"></b></a>
						<ul class="dropdown-menu" role="menu" aria-labelledby="drop1">
							<% for ministerio in Ministerio.all.cache do %>
							<li>
								<a tabindex="-1" href="<%= ministerio_path(ministerio)%>"><%= ministerio.titulo %></a>
							</li>
							<% end %>
						</ul>
					</li>
					<li><a href="#">Missionário</a></li>
					<li><a href="#">Educacional</a></li>
					<li><a href="#">Agenda</a></li>
					<li><a href="#">Devocionais</a></li>
				</ul>
				<ul class="nav pull-right">
					<li>

						<% if !user_signed_in? %>
						<a class="pull-right" href="/login">
							<i class="icon-key"> </i> Administração
						</a>
						<% end %>
					</li>
				</ul>

				<% if user_signed_in? %>
				<ul class="nav pull-right">
					<li>
						<a href="/painel"><i class="icon-dashboard"> </i> Painel de controle</a>
					</li>
				</ul>
				<% end %>

				<form class="navbar-form pull-right">
					<input type="text" class="span2 pull-right" placeholder="Pesquisar">
				</form>

			</div>
		</div>

		<% unless flash[:notice].blank? %>
		<div class="alert-message success fade in out">
			<a class="close" data-dismiss="alert">x</a>
			<div>
				<h2><%= flash[:notice] %></h2>
			</div>
		</div>
		<% end %>

		<% unless flash[:error].blank? %>
		<div class="alert-message error fade in out">
			<a class="close" data-dismiss="alert">x</a>
			<div>
				<h2><%= flash[:error] %></h2>
			</div>
		</div>
		<% end %>

		<% unless flash[:alert].blank? %>
		<div class="alert-message warning fade in out">
			<a class="close" data-dismiss="alert">x</a>
			<div>
				<h2><%= flash[:alert] %></h2>
			</div>
		</div>
		<% end %>

		<%= yield %>

	</div>

	<script type="text/javascript">
	$(document).ready(function() {
		$('.check_admin').iphoneStyle({
			checkedLabel: 'Admin.',
			uncheckedLabel: 'Comum'
		});
	});
	</script>

	<script type="text/javascript">
	$('.weditor').wysihtml5();
	</script>
</body>
</html>
